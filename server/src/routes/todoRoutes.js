// File: server/src/routes/todoRoutes.js
// Full implementation coming in the Todo step

const express = require("express");
const router = express.Router();

// Placeholder — will be replaced with full todo routes
router.get("/", (req, res) => {
  res.json({ message: "Todo routes coming soon" });
});

module.exports = router;
