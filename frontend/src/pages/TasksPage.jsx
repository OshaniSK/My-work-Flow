// src/pages/TasksPage.jsx — placeholder, wire up SearchFilter + TaskList + Modal
import React from "react";
const TasksPage = () => (
  <div>
    <h1 className="text-2xl font-bold text-slate-800 dark:text-white mb-6">All Tasks</h1>
    <p className="text-slate-500 dark:text-slate-400">Connect SearchFilter, TaskList, and Modal here.</p>
  </div>
);
export default TasksPage;
